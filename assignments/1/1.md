After completing 02-mnist_layers_activations:

    Extend this task so that the resulting accuracy of the model with the given parameters is determined five times (random seed is initialized only once at the beginning, then repeatedly construct and train the model from scratch).
    Calculate the mean and sample standard deviation of the accuracy results (on test data).
    Write the mean and sample standard deviation to a file outputValue.txt in the current directory (only the numbers, no other text; the mean value on the first line, the sample standard deviation on the second line, both exactly with six decimal places). 
    Return these numbers as a tuple from the main module's main procedure.

Submit the code as a single .py file. Check your code using the provided 01_code_check.py to avoid common mistakes preventing to get the points. 

You will receive 10 points for successful completion by the due date. You will receive only 5 points for successful completion after the due date but before the cut-off date (June 3, 2026, 08:00 a.m.).